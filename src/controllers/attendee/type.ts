export interface registerAttendee {
  attendeeId: string;
  name: string;
  profile: string;
  email: string;
  password: string;
  country: string;
  state: string;
  city: string;
}

export interface attendeeId {
  attendeeId: string;
}
